[project]
name = "pmdf-estudos"
version = "0.1.0"
description = "Dashboard de estudos PMDF CFO 2025"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.32.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
